TestLogin.java

package com.login;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.chrome.ChromeOptions;
import org.testng.annotations.BeforeSuite;
import org.testng.annotations.DataProvider;
import org.testng.annotations.Test;

import com.hms.login.page.HMSLoginPage;

Run All
public class TestLogin {
	WebDriver driver;

	@BeforeSuite
	public void luanchHMSApp() {
		System.setProperty("webdriver.chrome.driver",
				"C:\\Users\\ADMIN\\Downloads\\chromedriver_win32 (1)\\chromedriver.exe");
		ChromeOptions chromeOptions = new ChromeOptions();
		chromeOptions.addArguments("--remote-allow-origins=*");
		driver = new ChromeDriver(chromeOptions);
		driver.get("https://www.kosmiktechnologies.com/seleniumLiveProject/kosmik-hms/index.php");
		driver.manage().window().maximize();
	}

	@Test(dataProvider = "testData")
	Run | Debug
	public void testLogin(String user,String pwd) {

		HMSLoginPage page = new HMSLoginPage(driver);
//		Object[][] testData = { { "admin", "kosmik7" }, { "kosmik", "kosmik" } };
//		for (int i = 0; i < testData.length; i++) {
//			for (int j = 0; j < testData[i].length;) {
//				page.hmsLogin(testData[i][j].toString(), testData[i][j + 1].toString());
//				break;
//
//			}
//		}
		page.hmsLogin(user, pwd);
	}

	@DataProvider(name = "testData")
	public Object[][] testData() {
		Object[][] testData = { { "admin", "kosmik7" }, { "kosmik", "kosmik" } };
		return testData;

	}
	@AfterSuite
	public void closeBrowser() {
		driver.quit();
	}

}
